Task 1: complete (commits f64da3b..23eaf94, checkpoint review findings resolved, pushed)
Task 2A: complete (commit 2fecfce, spec compliance pass, code quality pass)
Task 2: complete (mandatory fixture, explicit diagnostic ignores, failure classification, and release-launcher contracts reconciled)
Task 3 baseline: seven focused scheduler contracts are RED (aerial entry animation, Attack100 chain, Dash completion, moonwalk carry, Turn special redispatch, RebirthWait handoff physics, DamageFly exit).
Task 3 source invariants: global HSD priority phases are p0 hitlag expiry, p1 JObj/AObj+script+anim_cb, p3 input_cb, p4 phys/integration, p6 coll_cb, p8/p9 accessories, p14 hit processing. Callback slots are reread by later phases after state changes; the current slot is not recursively re-entered unless a source helper explicitly requests animation evaluation.
Task 3 artifact audit: generic runtime bake preserves raw FigaTree bytes but rounds end frames and omits rewind/controller state; Falcon core generator preserves f32 end/flags but synthesizes rewind=0. Persistent primary/secondary AObj state belongs in mole_core rollback, while JObj matrices remain deterministic derivatives.
Task 6 audit: gameplay/rollback are already integer Frame at 60 Hz, but Friend Connect couples receive/input/sim/send/render into one nominal 60 Hz loop. A later HostCadence layer should support integer multiples 1/2/3/4 (60/120/180/240 Hz), allow receive/provisional input/correction every host pass, and call gameplay exactly once at each 60 Hz boundary. Corrected/late inputs trigger whole-frame rollback; checksums verify equivalent-frame convergence and hard-stop true desync rather than repairing state. Cross-cadence checksums/replay bytes must be identical. Transport currently cannot replace newer same-frame provisional revisions because sequence and duplicate ownership are frame-based.
Task 5 audit: whole-frame rollback shape is sound, but production gaps are (P0) no explicit checksum_frame/equivalent-frame hard comparison, (P0) no build/artifact/config compatibility handshake, (P0) native replay bypasses the runtime collision-aware frame executor, (P1) resimulation allocations and linear action scans, (P1) unbounded transport inbox history, and separate snapshot/checksum ownership. Implement in that dependency order after parity suites are reconciled.
Task 3A initial implementation: commits 0532920 and 1178007. Review found incomplete production legacy/playback synchronization, HSD_AObjReqAnim flag preservation conflated with descriptor installation, and incomplete secondary-field checksum assertions. Remediation is in progress; do not build Task 3B atop the initial API until clean rereview.
Task 3B staged design: commit families in order (1) global p1 animation/script/anim callbacks plus playback authority for Dash/Attack100, (2) global p3 input and explicit entry evaluation for AttackAir/Turn, (3) global p4 physics/integration for RebirthWait handoff, (4) global p6 map/collision plus damage phase split, (5) remove old same-tick flags and monolith branches. Preserve capture/throw events through deterministic phase barriers.
Task 3A: complete after commits 03dfb92, 46c7603, and ebbc683. Fresh rereview PASS: exact AObj request semantics, persistent snapshot/checksum coverage, production and diagnostic legacy synchronization, 78/78 mole_core lib tests, formatting and diff checks.
Task 3B4 damage audit: current advance_source_damage_state combines p1/p3/p4/p6 and uses a post-animation ECB exception. Split into phase-neutral damage anim, input dispatch, physics, and map primitives. P1 owns hitstun decrement/unlock and DamageFly completion; p3 observes same-tick unlock; p4/p6 reread current state. P6 decrements ECB lock before live-pose load and preserves only desired bottom while locked. Remove action-specific post-anim pose sampling after phase publication exists.
Task 3B3 rebirth/p4 audit: extract a global p4 loop ending immediately before resolve_ground_support_after_move/current sim.rs p6 boundary. Rebirth/RebirthWait p1 and p3 only transition; p4 rereads live state and owns state physics plus common velocity/position integration. Remove fall_entered_from_rebirth_wait_this_tick and combined helper boolean provenance, but retain broader unmigrated skip flags until their owners migrate. Per-player phase scratch is ephemeral fixed-size state, not rollback-owned.
Task 3B2 p3 audit: reuse one callable evaluate_active_animation primitive for both ordinary p1 evaluation and explicit entry calls. AttackAir p3 entry evaluates new AObj+command stream once but never calls new anim_cb; p4/p6 use new callbacks. Turn p1 flips/sets just_turned, then p3 ORs latched buttons into current input and current stick, latching current A/B only after higher-priority checks fail. Remove sample_source_motion_frame_for_action_entry once shared evaluation covers it.
Task 6 resolver requirement: peers may independently sustain different host cadences (120/180/240) while sharing 60 Hz authoritative frame IDs. Select the highest consistently maintainable integer multiplier from sustained deadline performance, using a conservative floor and hysteresis rather than peak FPS. Do not negotiate all peers down to one render cadence. Separate input/network/rollback host passes from presentation where possible. Acceptance includes mixed-cadence deterministic checksums and release-mode allocation/timing benchmarks targeting easy 240 Hz on low-end hardware.
Rollback source rule: use checked-in Slippi/Dolphin research plus existing native rollback docs as source truth. Preserve observable protocol/scheduler invariants without importing whole-emulator savestate overhead into the native engine. Every rollback/cadence task must cite local reference evidence, add source-to-Rust contracts, and update architecture docs in the same task.
Task 3B1 review remediation: complete. Attack100 Start now runs ChangeMotionState first-play plus explicit entry `ftAnim`; Start->Loop and Loop->End immediately evaluate the new AObj/script without recursive callback dispatch; Loop start is published by the first ordinary Loop p1 callback. Replaced the synthetic common-ID-48/frame-40 fixture with source-keyed rows 6/14/22/30/37 from Falcon action-table index 50, subaction offset 0x4710, and its checked-in PlCa subroutine; Mole CLI owns the decoder proof while core keeps only compact fixture metadata. Focused contracts, 81/81 `mole_core --lib`, and canonical strict replay all pass with 5,313 comparisons and zero divergence.
Task 5 rollback audit refinement: use one fixed-capacity authoritative frame ring containing pre-frame snapshot, input confirmation state, and post-frame checksum. Corrections restore the pre-frame snapshot and rebuild every record/checksum through the present cursor via the configured production StepWorldFn. Add independent input revision and checksum_frame identities, hard-stop only equivalent finalized-frame checksum mismatches, gate frame zero behind protocol/build/artifact/session-config compatibility, route replay through the collision-aware executor, and bound transport/history/allocation hot paths. Slippi's seven-frame window and bounded savestate semantics are source evidence; native snapshots remain minimal World state.
Task 6 cadence audit refinement: authoritative gameplay remains exactly 60 Hz. Local host passes independently run at multiplier 1/2/3/4 with distinct HostPassId, authoritative Frame, and wire InputRevision identities. Receive/input revision/correction can run each host pass; gameplay advances only at the 60 Hz boundary; presentation is non-authoritative. Resolver policy uses sustained complete-pass floors with slow promotion and faster hysteretic demotion, never peer-wide cadence negotiation. Explicit checksum_frame and bounded newer same-frame revision support are prerequisites.
Task 3 strict-fixture scheduler milestone: global p0 hitlag expiry, p1 Damage/AttackAir/Fall animation callbacks, p3 Turn/AttackAir input callbacks, and the strict fixture's later callback rereads now follow the decomp priority topology. Common Damage IDs 75-86 and DamageFly IDs 87-91 retain separate source callback-family pose timing. Focused source frames 2351, 3542, 4462, and 4820 pass, and the strict 5,313-frame Falcon/Battlefield fixture completes with zero engine divergence. Full workspace-wide p4/p6 phase extraction remains open and must not be inferred from this scoped result.
Task 5 current truth: obsolete frame-2583/2586 comparison expectations now require no divergence through frame 3200; the visual gate does not fabricate a stop. `mole_core` library is 83/83, collision contract is 11/11, and broad core contract is 580 passed/41 failed/2 ignored. The remaining failures stay explicitly classified and prevent a universal parity claim.
